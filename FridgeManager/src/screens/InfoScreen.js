import { View, Text } from "react-native";
import { useRoute } from "@react-navigation/native";

const InfoScreen = () => {
    const route = useRoute();

    return (
        <View>
            <Text></Text>
        </View>
    );
};